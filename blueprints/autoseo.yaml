form:
  fields:
    tabs:
      fields:
        autoSeo:
          type: tab
          title: SEO

          fields:
            header.autoseo.enabled:
              type: toggle
              label: PLUGINS.AUTO_SEO.ENABLE_PLUGIN
              highlight: 1
              default: 1
              options:
                1: PLUGIN_ADMIN.ENABLED
                0: PLUGIN_ADMIN.DISABLED
              validate:
                type: bool
